import React from 'react';
import { directive } from '@babel/types';

export default function Header(props){
    return(
        <header className="header">
            <h1>Generic Twitter App!</h1>
        </header>
    )
}